name: tdd_intro
description: A new Flutter application.


version: 1.0.0+1

environment:
  sdk: ">=2.2.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  equatable: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^2.2.0
  mockito: ^5.2.0

flutter:

  uses-material-design: true
